"select
         r.store_id,
         r.rank_id,
         r2.rank_id as rank_id_other
 from
         rank r
 join
         rank r2
 on
         r.store_id = r2.store_id
 and
         r.rank_id != r2.rank_id
 order by
         r.store_id,
         r.rank_id,
         r2.rank_id"